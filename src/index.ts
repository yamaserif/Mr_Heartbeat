  const routes = require('./routes/routes')
  routes.setRoutes()

  console.log('「http://127.0.0.1:3000/」にアクセスしてください')
  console.log('また、心拍数の表示画面は「http://127.0.0.1:3000/heart-view」になりますので、OBS等に読み込ませる場合はこちらのURLを入力してください。')